# TicTacToe with NumPy + Streamlit

A terminal and web-based TicTacToe game built with NumPy for game logic and Streamlit for the UI.

## Run locally
pip install -r requirements.txt
streamlit practice.py
